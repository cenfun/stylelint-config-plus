const stylelintStandardRules = require('stylelint-config-standard').rules;

const standardRules = {
    ... stylelintStandardRules

    // overrides
    // none for now

};

module.exports = standardRules;
